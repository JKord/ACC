{% extends 'ACCAdminBundle::layout.html.twig' %}
{% block title %}Workers{% endblock %}

{% block right%}
<div class="section">
	<div class="box">
		<div class="title">
			Workers
			<span class="hide"></span>
		</div>
		<div class="content">
<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>ПІ</th>
            <td>{{ entity.name }}</td>
        </tr>
        <tr>
            <th>Посада</th>
            <td>{{ entity.post }}</td>
        </tr>
    </tbody>
</table>
			<div class="row">
				<a href="{{ path('workers') }}" class="btn">Назад до списку</a>
				<a href="{{ path('workers_edit', { 'id': entity.id }) }}" class="btn">Edit</a>
				<form action="{{ path('workers_delete', { 'id': entity.id }) }}" method="post" class="form_inline">
            		{{ form_widget(delete_form) }}
            		<button type="submit" class="btn">Видалити</button>
        		</form>
			</div>
		</div>
	</div>
</div>
{% endblock %}