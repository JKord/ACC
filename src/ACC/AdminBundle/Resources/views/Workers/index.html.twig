{% extends 'ACCAdminBundle::layout.html.twig' %}
{% block title %}Workers list{% endblock %}

{% block right%}
<div class="section">
	<div class="box">
		<div class="title">
			Workers list
			<span class="hide"></span>
		</div>
		<div class="content">
<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>ПІ</th>
            <th>Посада</th>
            <th>Дії</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('workers_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.name }}</td>
            <td>{{ entity.post }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('workers_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('workers_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
<ul>
    <li>
        <a href="{{ path('workers_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
		</div>
	</div>
</div>
{% endblock %}