{% extends 'ACCAdminBundle::layout.html.twig' %}
{% block title %}Workers creation{% endblock %}

{% form_theme edit_form 'ACCAdminBundle:Form:row_div.html.twig' %}

{% block right%}
<div class="section">
	<div class="box">
		<div class="title">
			Workers creation
			<span class="hide"></span>
		</div>
		<div class="content">
<form action="{{ path('workers_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>
			<div class="row">
				<a href="{{ path('workers') }}" class="btn">Назад до списку</a>  
			</div>
		</div>
	</div>
</div>
{% endblock %}