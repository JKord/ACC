{% extends 'ACCAdminBundle::layout.html.twig' %}
{% block title %}Groups creation{% endblock %}

{% form_theme form 'ACCAdminBundle:Form:row_div.html.twig' %}

{% block right%}
<div class="section">
	<div class="box">
		<div class="title">
			Groups creation
			<span class="hide"></span>
		</div>
		<div class="content">
<form action="{{ path('groups_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit" class="btn">Create</button>
    </p>
</form>
			<div class="row">
				<a href="{{ path('groups') }}" class="btn">Назад до списку</a>  
			</div>
		</div>
	</div>
</div>
{% endblock %}