{% extends 'ACCAdminBundle::layout.html.twig' %}
{% block title %}Area{% endblock %}

{% block right%}
<div class="section">
	<div class="box">
		<div class="title">
			Area
			<span class="hide"></span>
		</div>
		<div class="content">
<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Будівельний департамент</th>
            <td>{{ entity.idBuildingDepartments }}</td>
        </tr>
        <tr>
            <th>Група</th>
            <td>{{ entity.idGroup }}</td>
        <tr>
            <th>Керівник ділянки</th>
            <td>{{ entity.idWorker }}</td>
        </tr>
    </tbody>
</table>
			<div class="row">
				<a href="{{ path('area') }}" class="btn">Назад до списку</a>
				<a href="{{ path('area_edit', { 'id': entity.id }) }}" class="btn">Edit</a>
				<form action="{{ path('area_delete', { 'id': entity.id }) }}" method="post" class="form_inline">
            		{{ form_widget(delete_form) }}
            		<button type="submit" class="btn">Видалити</button>
        		</form>
			</div>
		</div>
	</div>
</div>
{% endblock %}